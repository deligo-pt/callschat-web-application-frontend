Description
After successful Web QR Login and Key Sync, the Web application intermittently shows Encrypted Message (Decryption Failed) or Error: incorrect key pair for the given ciphertext when decrypting messages.

Root Cause Analysis
Encryption Scheme Mismatch: The mobile app encrypts key sync / messages using pure-Dart X25519 + XChaCha20-Poly1305 (cryptography package). If the web client uses Libsodium/TweetNaCl crypto_box_open_easy (which expects XSalsa20-Poly1305 + HSalsa20 key derivation), decryption fails due to MAC/cipher tag structure differences.
Device Key Rotation Sync: When a user logs in via QR on Web, the Web client needs to store and use the exact X25519 private key seed belonging to the active mobile session, and correctly handle peer public key cache refreshes.
Proposed Solution / Requirements
Web E2EE Library Alignment: Ensure the Web client's decryption helper supports XChaCha20-Poly1305 / 24-byte nonces used by the mobile app, OR standardize key derivation across web and mobile.
Fallback Key Resolution: On receiving chat:receive_message, if initial crypto_box_open_easy fails, automatically re-fetch the sender's latest public key (GET /api/v1/encryption/keys/:userId) before marking message as failed.
Key Sync Validation: Add log verification on Web client after QR auth success: verify userPrivateKey in localStorage matches mobile key signature.
ওয়েব অ্যাপ্লিকেশনে (React, Next.js, Vue বা Vanilla JS) মোবাইল অ্যাপের সাথে ১০০% সামঞ্জস্যপূর্ণ (Compatible) End-to-End Encryption (E2EE) ইমপ্লিমেন্ট করার জন্য একটি বিস্তারিত টেকনিক্যাল ডিসকাশন ও গাইড নিচে দেওয়া হলো:

🌐 Web E2EE Implementation Discussion & Architecture Guide
১. মূল উদ্দেশ্য (Objective)
আমাদের Flutter মোবাইল অ্যাপ X25519 (Key Exchange) এবং XChaCha20-Poly1305 (AEAD Encryption) অ্যালগরিদম ব্যবহার করছে।
ওয়েব অ্যাপেও হুবহু একই অ্যালগরিদম ব্যবহার করতে হবে যেন:

Mobile ↔ Web: মোবাইল থেকে পাঠানো মেসেজ ওয়েবে ডিক্রিপ্ট করা যায়।
Web ↔ Web: ওয়েব থেকে পাঠানো মেসেজ ওয়েব বা মোবাইলে পড়া যায়।
২. ওয়েব লাইব্রেরি নির্বাচন (Recommended JavaScript Libraries)
ব্রাউজারের নেটিভ WebCrypto API তে কিছু পুরনো ব্রাউজারে XChaCha20-Poly1305 এবং X25519 সরাসরি সাপোর্টে সীমাবদ্ধতা থাকতে পারে। তাই ১০০% বাইট-টু-বাইট ক্রিপ্টোগ্রাফিক সামঞ্জস্যতার জন্য @noble প্যাকেজ অথবা libsodium-wrappers ব্যবহার করা সবচেয়ে বেস্ট চয়েস।

Recommended Stack (JS/TS):
@noble/curves: X25519 Key Exchange এর জন্য।
@noble/ciphers: XChaCha20-Poly1305 এনক্রিপশনের জন্য।
(অথবা libsodium-wrappers: WebAssembly ভিত্তিক Libsodium)।
npm install @noble/curves @noble/ciphers @noble/hashes
৩. স্টেপ-বাই-স্টেপ ইমপ্লিমেন্টেশন প্ল্যান
Step 1: Keypair Generation & Shared Secret (X25519)
ওয়েব অ্যাপ চালু হলে যদি ডিভাইস কি না থাকে, তবে একটি নতুন X25519 কি- পেয়ার তৈরি হবে:

import { x25519 } from '@noble/curves/ed25519';
import { bytesToBase64, base64ToBytes } from '@noble/hashes/utils';

// ১. নতুন Keypair জেনারেট
export function generateKeyPair() {
  const privateKey = x25519.utils.randomPrivateKey(); // 32 bytes Uint8Array
  const publicKey = x25519.getPublicKey(privateKey);  // 32 bytes Uint8Array

  return {
    privateKeyBase64: bytesToBase64(privateKey),
    publicKeyBase64: bytesToBase64(publicKey)
  };
}

// ২. Shared Secret বের করা (My Private Key + Peer Public Key)
export function deriveSharedSecret(myPrivateKeyBase64: string, peerPublicKeyBase64: string): Uint8Array {
  const myPrivateKey = base64ToBytes(myPrivateKeyBase64);
  const peerPublicKey = base64ToBytes(peerPublicKeyBase64);

  // X25519 Diffie-Hellman Calculation
  const sharedSecret = x25519.getSharedSecret(myPrivateKey, peerPublicKey);
  return sharedSecret; // 32 bytes Shared Secret
}
Step 2: Encrypt & Decrypt Message (XChaCha20-Poly1305)
মোবাইলের cryptography প্যাকেজ ২৪-বাইটের Nonce এবং ১৬-বাইটের MAC tag ব্যবহার করে। ওয়েবেও হুবহু একই স্ট্রাকচারে এনক্রিপ্ট/ডিক্রিপ্ট করতে হবে:

import { xchacha20poly1305 } from '@noble/ciphers/chacha';
import { randomBytes } from '@noble/ciphers/webcrypto';
import { bytesToBase64, base64ToBytes, utf8ToBytes, bytesToUtf8 } from '@noble/hashes/utils';

// ৩. মেসেজ এনক্রিপশন (Encrypt)
export function encryptMessage(plaintext: string, sharedSecret: Uint8Array) {
  const nonce = randomBytes(24); // 24 bytes random nonce for XChaCha20
  const cipher = xchacha20poly1305(sharedSecret, nonce);

  const plaintextBytes = utf8ToBytes(plaintext);
  const encryptedPayload = cipher.encrypt(plaintextBytes); 
  // encryptedPayload contains (ciphertext + 16-byte Poly1305 MAC)

  // MAC tag আলাদা করা (শেষ ১৬ বাইট)
  const ciphertextBytes = encryptedPayload.slice(0, encryptedPayload.length - 16);
  const macBytes = encryptedPayload.slice(encryptedPayload.length - 16);

  return {
    ciphertext: bytesToBase64(encryptedPayload), // Combined (mobile matching)
    nonce: bytesToBase64(nonce),
    mac: bytesToBase64(macBytes)
  };
}

// ৪. মেসেজ ডিক্রিপশন (Decrypt)
export function decryptMessage(
  ciphertextBase64: string,
  nonceBase64: string,
  macBase64: string,
  sharedSecret: Uint8Array
): string | null {
  try {
    const rawBytes = base64ToBytes(ciphertextBase64);
    const nonce = base64ToBytes(nonceBase64);
    const cipher = xchacha20poly1305(sharedSecret, nonce);

    // ডিক্রিপ্ট করা (Combined payload/ciphertext + MAC)
    const decryptedBytes = cipher.decrypt(rawBytes);
    return bytesToUtf8(decryptedBytes);
  } catch (err) {
    console.error("Decryption failed:", err);
    return null;
  }
}
Step 3: Secure Key Storage in Browser (ব্রাউজার সিকিউরিটি)
মোবাইলে যেমন Keychain/KeyStore থাকে, ব্রাউজারে তা নেই। তাই ব্রাউজারে নিরাপত্তা নিশ্চিতে:

স্টোরেজ অপশন	সিকিউরিটি লেভেল	মন্তব্য
localStorage	⚠️ কম (XSS Risk)	কোনো XSS স্ক্রিপ্ট রান হলে প্রাইভেট কী চুরি হতে পারে।
IndexedDB	🟡 মাঝারি	সিকিউর স্টোরেজের জন্য localStorage এর চেয়ে ভালো।
IndexedDB + WebCrypto / User Session Derivation	🟢 সবচেয়ে নিরাপদ	Private Key-কে সেশন পাসওয়ার্ড বা ডেরিভড কী দিয়ে এনক্রিপ্ট করে IndexedDB তে রাখা।
Step 4: Multi-Device & Socket Workflow
ডিভাইস রেজিস্ট্রেশন:

ওয়েব ব্রাউজারের জন্য একটি নতুন deviceId জেনারেট হবে (uuidv4())।
ওয়েবের Public Key টি API ইমপয়েন্ট POST /api/v1/encryption/keys এ আপলোড হবে।
মেসেজ আদান-প্রদান (Socket.io / WebSocket):

চ্যাট ওপেন হলে পিয়ার ইউজারের পাবলিক কী ফেচ করা হবে: GET /api/v1/encryption/keys/{peerUserId}।
deriveSharedSecret() দিয়ে Shared Secret মেমোরিতে রাখা হবে।
মেসেজ পাঠানোর সময় মোবাইলের মতোই ciphertext, nonce, mac সহ Payload সকেটে ইনপুট দেওয়া হবে।
💡 চার্ট: মোবাইল ও ওয়েব চ্যাট ফ্লো (Architecture Flowchart)

🔒 গুরুত্বপূর্ন সিকিউরিটি চেকপয়েন্ট (Checkpoints)
Base64 String Encoding Standard:
নিশ্চিত করতে হবে Flutter এবং Web উভয় স্থানেই স্ট্যান্ডার্ড Base64 (padding সহ) ব্যবহার হচ্ছে।
Byte Order & MAC Tag Handling:
Flutter অ্যাপে cryptography প্যাকেজ Payload-এর শেষ ১৬ বাইটে MAC Tag যুক্ত করে। JavaScript ডিক্রিপ্ট করার সময় Combined Payload (Ciphertext + 16-byte MAC) হুবহু রিড করছে কি না তা নিশ্চিত করতে হবে।
Session Secret Cache:
প্রতিটি মেসেজে বার বার Diffie-Hellman ক্যালকুলেশন না করে চ্যাট সেশন চলাকালীন Shared Secret মেমোরিতে ক্যাশ (Cache) করে রাখা ভালো (যেমনটি মোবাইলে shared_secret_$conversationId দিয়ে রাখা হয়েছে)।
